TODO
=====




DONE
=====
* make functions to handle the appearance of date timestamps
* write custom function to count comments on a post
* add category and author information on each post
* Add the single post template
* Improve the queries we wrote last time to include:
	- a count of posts in each category
* list all the approved comments on single
* make the comment form
* parse new comments

* make a function to display the userpic

* build the search form
* build the search results page
* add pagination to "flip" through the results

* build the registration form 
* build the login form
* make the "login/register" buttons change to "user account/log out" button
* make the logout logic
* make the comment form use the logged in user
* make the "new post" page

* Make a function to display uploaded images at any size
* Finish the "edit post" process (step 2 of "new post")
* Make an edit button so users can edit their own posts

* Import the likes table (likes.sql) into our DB
* write the HTML for the like button interface (index.php)
* Add the CSS for the heart button (css/style.css)
* Add the count_likes function (includes/functions.php)
* add the likes interface function (includes/functions.php)
* add the JS Fetch() call when the user clicks the heart button (includes/footer.php)
* add the ajax handler PHP file to update the DB behind the scenes (ajax-handlers/like-unlike.php)